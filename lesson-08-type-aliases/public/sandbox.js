"use strict";
const logDetails = (uid, item) => {
    console.log(`${item} has uid of ${uid}`);
};
const logDetailsAgain = (uid, item) => {
    console.log(`${item} has uid of ${uid}`);
};
const greet = (user) => {
    return `${user.name} says hi to you.`;
};
const greetAgain = (user) => {
    return `${user.name} says hi to you again.`;
};
